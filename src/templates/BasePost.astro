---
// eslint-disable-next-line prettier/prettier
import { BlogPost } from '@/partials/BlogPost';
import Base from '@/templates/Base.astro';
import type { IFrontmatter } from '@/types/IFrontMatter';
import { AppConfig } from '@/utils/AppConfig';

export interface Props {
  content: IFrontmatter;
}

const { content } = Astro.props as Props;
const title = `${content.title} - ${AppConfig.site_name}`;
const ogImage = content.imgSrc; // Use imgSrc from frontmatter
---

<Base head={{ title, description: content.description, ogImage }}>
  <BlogPost frontmatter={content}>
    <slot />
  </BlogPost>
</Base>
